#!/usr/bin/env sh
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
PY="$ROOT/python/python"
if [ ! -x "$PY" ]; then
  PY="$ROOT/venv/bin/python"
fi
if [ ! -x "$PY" ]; then
  PY="$ROOT/venv/Scripts/python.exe"
fi
exec "$PY" -c "from agent_reach.cli import main; main()" "$@"
